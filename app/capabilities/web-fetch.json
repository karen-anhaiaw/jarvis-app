{
  "name": "web_fetch_local",
  "category": "web",
  "description": "[LOCAL — FALLBACK] Web page fetch running on this machine. Returns the page content as readable text (HTML converted to markdown, no citations). PREFER the server-side `web_fetch` tool for normal use — only use this one when the user explicitly asks for a local fetch, or when `web_fetch` is unavailable or failing.",
  "type": "script",
  "command": "bash",
  "args": [
    "capabilities/scripts/web-fetch.sh",
    "${url}",
    "${max_length}"
  ],
  "input_schema": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "The URL to fetch"
      },
      "max_length": {
        "type": "number",
        "description": "Maximum characters to return (default: 10000, max: 50000)"
      }
    },
    "required": [
      "url"
    ]
  }
}
